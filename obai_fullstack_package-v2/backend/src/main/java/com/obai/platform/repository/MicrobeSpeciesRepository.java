package com.obai.platform.repository;

import com.obai.platform.entity.MicrobeSpecies;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MicrobeSpeciesRepository extends JpaRepository<MicrobeSpecies, Long> {
}
