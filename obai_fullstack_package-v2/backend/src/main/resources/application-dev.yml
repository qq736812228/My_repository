spring:
  datasource:
    url: ${OBAI_DB_URL:jdbc:postgresql://localhost:5432/obai}
    username: ${OBAI_DB_USERNAME:obai}
    password: ${OBAI_DB_PASSWORD:obai_dev_password}
    driver-class-name: org.postgresql.Driver
logging:
  level:
    com.obai.platform: DEBUG
    org.hibernate.SQL: INFO
